method main() {
    var M: Int
    var N: Int
    var res: Int

    assume N > 0 && M >= 0
    
    var m: Int := M

    res := 0

    while (m > 0) 
    invariant M == res * N + m
    {
        m := m - N
        res := res + 1
    }

    assert M == res * N + m
}